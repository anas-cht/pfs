# Core
streamlit
PyMuPDF
asyncio
concurrent.futures

# LangChain ecosystem
langchain
langchain-core
langchain-community
langchain-text-splitters
langchain-huggingface
langchain-ollama

# Embeddings and Vector DB
sentence-transformers
faiss-cpu  # Use faiss-gpu if you have GPU

# Optional (for local LLMs like Ollama)
ollama  # Needed if you run models locally via Ollama

# Ensure frontend conflict is avoided
# Do NOT include 'fitz' package separately


# Run:   pip install -r requirements.txt
